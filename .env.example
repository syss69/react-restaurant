CONTENTFUL_SPACE_ID=
CONTENTFUL_ACCESS_TOKEN=

# Administration (/admin) — mot de passe pour la première couche d’accès
ADMIN_PASSWORD=
# Optionnel : secret dédié pour signer la session (sinon ADMIN_PASSWORD est utilisé)
# ADMIN_SESSION_SECRET=

# Supabase — réservations (dates bloquées). Sans ces variables, stockage local data/blocked-dates.json
# Exécuter supabase/migrations/20250411000000_reservation_settings.sql dans le SQL Editor.
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

